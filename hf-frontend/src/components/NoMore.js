import React from 'react'

const NoMore = ({input}) => {
    return (
        <div className="no__more__container">
            <p className = 'no__more'>No More {input} to Show</p>
        </div>
    )
}

export default NoMore
